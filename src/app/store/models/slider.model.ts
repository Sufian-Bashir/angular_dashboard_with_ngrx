import { SliderDetails } from "./tab.model";

export class Slider implements SliderDetails {
    public value: number;
    public highValue: number;
    public floor: number;
    public ceil: number;

}